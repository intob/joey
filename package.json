{
  "name": "joeyinnes",
  "private": true,
  "version": "0.0.2",
  "scripts": {
    "start": "web-dev-server --open --watch --app-index index.html"
  },
  "devDependencies": {
    "@web/dev-server": "latest"
  }
}
